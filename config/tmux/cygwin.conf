# pane navigation
bind -n C-h select-pane -L
bind -n C-j select-pane -D
bind -n C-k select-pane -U
bind -n C-l select-pane -R
bind -n C-\ select-pane -l

# yank to windows clipboard
bind -t vi-copy "y" copy-pipe "putclip -n"
